Final integration (micro-BFF, HttpClient, and validation)
Context: The `bragGeneratorFlow` already exists. The interface is already in place, currently using a service with mocked data.

Instructions for the Agent:
1 - Use the Genkit MCP: Consult the local context if necessary to ensure correct syntax.

2 - Secure backend (server.ts):
- Modify the Angular `server.ts` file.
- Immediately after initializing the Express app (`const server = express()`), add `server.use(express.json())`.
- Create a route `server.post('/api/brag', async (req, res) => { ... })` before the Angular catch-all route (`server.get('**')`).
- Inside the route, extract `req.body.definition`.
- Invoke `bragGeneratorFlow`, passing exactly the object `{ definition: req.body.definition }`.
- Return the result as JSON. In case of error, return HTTP status 500.

3 - Angular configuration (app.config.ts):
- Modify `src/app/app.config.ts` to provide `HttpClient`.
- Use `provideHttpClient(withFetch())` to ensure seamless SSR compatibility.

4 - Service update (brag.service.ts):
- Completely remove the method that generates mocked data.
- Inject `HttpClient`.
- Create the actual method `generateBrag(definition: string)`.
- The method should: set the `loading` signal to `true`, perform a POST request to `api/brag` sending `{ definition }`, add the API result to the `brags` list signal, and set `loading` to `false`. Handle potential errors.

5 - Build validation (critical):
- After implementing all the changes above, run the project's build command in the terminal (e.g., `npm run build`).
- Check the terminal output. If Angular reports any compilation errors (TypeScript, missing dependencies, SSR failure), automatically fix the code until the build succeeds.