/opsx:new change-id: add cfp feature
Objective: Implement the talk submission module (Call for Papers).
Strict architectural rules:
Frontend: Angular 21 in `apps/frontend`. Mandatory use of Standalone Components, Signals for state management, and WAI-ARIA for accessibility.
Backend: NestJS in `apps/api`. Use of `@Body()` with strict validation via `class-validator`.
Shared: Both must consume the `SpeakerDTO` contract exported from the `shared-types` library.
Quality (Mandatory unit tests): The plan must include the creation of tests using Jest. NestJS requires tests ensuring invalid payloads are rejected (400 Bad Request). Angular requires tests validating the initial Signal state and the disabling of the submit button.
Generate the files `proposal.md`, `design.md`, and `tasks.md`. Do not write any application or test code yet.