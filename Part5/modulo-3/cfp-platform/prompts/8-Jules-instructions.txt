@jules, act as a senior full-stack developer.
Implement the "Event Venue Registration" feature in our Nx monorepo.

Mandatory Guidelines:
1 - Design System: Strictly adhere to the visual standards, colors, and Design Tokens already applied to the CFP form. The new form must look like part of the same system.
2 - Navigation: Update the navigation menu to include exactly these three options: "Event Registration" (/event/new), "Talk Registration" (/talks/new), and "Dashboard" (/dashboard).
3 - Architecture: Create a new DTO named `eventDTO` in `@cfp-platform/shared-types` containing: name, address, capacity, and date.
4 - Backend (NestJS): Implement the service/controller to handle the POST request and save this data to an in-memory array.
5 - Frontend (Angular 21): Create the component using Reactive Forms and Signals.

Create the branch, implement the code following the rules above, and open a Pull Request for my review.