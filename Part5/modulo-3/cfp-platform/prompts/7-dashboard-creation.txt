/opsx:new add-cfp-dashboard
Objective: Create a dashboard to list submitted talks.
Backend (NestJS): Add a GET route to `CfpController` that returns an array of `SpeakerDTO`.
Frontend (Angular 21): Create a `CfpDashboardComponent`. Inject `HttpClient` to implement the GET request. Manage state using Signals (`WritableSignal<SpeakerDTO>`).
UI/UX (Important): Use semantic HTML (table or list of cards) and strictly adhere to the visual identity, design, and colors (Design Tokens) established in the original form (`CfpFormComponent`) to ensure interface consistency.
Routing: Add a route (`path: 'dashboard'`) pointing to the component. Add a navigation button to the form, maintaining the same visual style.

Upon completion: /opsx:archive add-cfp-dashboard