/opsx:explore Read the current structure of our Nx monorepo via MCP. Locate the `api` and `frontend` apps and confirm the existence of the `shared-types` library containing our `SpeakerDTO`. Map the topology only; do not suggest code.