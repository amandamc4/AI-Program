Integrate `@pix-receipt.component.ts` into our main screen, `@pix-transfer.component.ts`.
Strictly follow the official Angular documentation (version 21) to apply best practices.
Update the logic for the confirm button: if the amount is valid and less than or equal to 5000, the transfer form should disappear and the
`PixReceiptComponent` should be rendered, receiving the data via `@Input`. Use the modern control flow syntax (`@if` / `@else`)
documented by Angular in the template to toggle between the screens.